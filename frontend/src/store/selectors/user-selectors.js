export const selectUser = ({ user }) => user;
export const selectUserRole = ({ user }) => user.role;
export const selectUserToken = ({ user }) => user.token;
export const selectIsAuth = ({ user }) => user.isAuth;
